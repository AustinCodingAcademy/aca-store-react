const express = require("express");
const router = express.Router();
// const show = require('../controllers/orders');

// router.get('/', show)
// router.post('/',create)

// router.post('/', createUser)
// module.exports = router;